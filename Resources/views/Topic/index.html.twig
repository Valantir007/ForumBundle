{% if topics %}
    {{ include('ValantirForumBundle:Topic:topics-list.html.twig', { 'topics': topics, 'readedTopics': readedTopics }) }}
    <div class="navigation">
        {{ knp_pagination_render(topics, 'ValantirForumBundle::twitter_bootstrap_pagination.html.twig') }}
    </div>
{% endif %}